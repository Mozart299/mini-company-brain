FROM golang:1.23-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bin/ingestion ./cmd/ingestion

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /bin/ingestion /bin/ingestion
ENTRYPOINT ["/bin/ingestion"]
